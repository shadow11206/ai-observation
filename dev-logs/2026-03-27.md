# 开发日志 · 2026-03-27

## 今日完成

### 1. UI 目录结构调整
- 原先错误地创建了独立 `ui` 分支，应为在 `main` 分支下建 `ui/` 目录
- 将 `ui` 分支内容合并到 `main`，删除本地 `ui` 分支，远端 `ui` 分支因是 default branch 需手动到 GitHub 删除

### 2. 日报页面架构升级（核心）

**方案确认：**
- 日报详情页采用 `report.html?date=YYYY-MM-DD` 单页动态加载，不生成独立静态文件
- 数据存储：`.md`（人类可读）+ `.json`（前端渲染）双格式同步输出

**新增文件：**
- `scripts/fetch_snapshot.py`：今日数据快照（Hugging Face Trending + GitHub AI 项目，无需 API Key）
- `ui/report.html`：日报详情页，左侧 6 模块导航 + 右侧内容区
- `ui/scripts/daily.js`：列表页动态加载 JSON，渲染摘要卡片
- `ui/scripts/report.js`：详情页 6 模块渲染 + 侧边栏滚动高亮

**修改文件：**
- `scripts/generate_daily.py`：重写，AI 输出结构化 JSON，同时保存 `.md` + `.json`
- `scripts/prompts/daily_report.txt`：改为结构化 JSON 输出 prompt
- `ui/daily.html`：列表页改为动态加载，不再是静态 HTML

**日报详情页 6 个模块：**
1. 📌 今日最重要（含一句话判断）
2. 🔬 模型 / 技术动态
3. 🏢 公司 / 产品动态
4. 💡 追踪人物观点（当前占位，Level 2 接入后开放）
5. 🔭 值得深挖？
6. ⚡ 今日数据快照（HF Trending + GitHub Trending）

### 3. 代码自检修复（9 项）

| 文件 | 问题 | 修复 |
|------|------|------|
| `daily.js` | `rank` 为 null 时输出 `0undefined` | 改用 `padStart(2,'0')` |
| `daily.js` | 列表卡片无原文链接 | 标题加链接 + 「原文 →」按钮 |
| `report.js` | rank 前缀硬编码 `0${rank}`，rank≥10 会错 | 同上 |
| `report.js` | `importance` 为 0/undefined 时星星显示空白 | 加 `Math.min/max(1,5)` 保护 |
| `report.js` | snapshot url 为空时生成 `<a href="">` 点击刷新页面 | 空 url 时渲染纯文本 |
| `fetch_snapshot.py` | GitHub 403 fallback 后盲目 `raise_for_status` | 独立 try/except，安全返回 [] |
| `fetch_snapshot.py` | `topic:llm topic:ai` AND 逻辑，命中率极低 | 改为 OR 逻辑 |
| `fetch_snapshot.py` | 两个请求共用 try/except，一个失败影响另一个 | 改为各自独立 |
| `generate_daily.py` | JSON 正则贪婪匹配可能截取错误 | 改为末尾锚定 `(\{[\s\S]*\})\s*$` |

---

## 遗留问题

- GitHub 远端 `ui` 分支需手动删除（因被设为 default branch，无法通过命令行删除）
- `ui` 分支的 GitHub Pages 配置：Source 需改为 `main` 分支 + `/ui` 目录

## 下一步待办

- [ ] 配置 GitHub Pages（main 分支 / ui 目录）
- [ ] 手动触发一次 GitHub Actions 日报，验证 JSON + 快照输出
- [ ] 追踪人物观点模块（Level 2 信息源接入后）

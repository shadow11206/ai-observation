# 开发日志 · 2026-03-29

## 今日完成

### 1. 项目全量流程图梳理

**背景：** 哥要求画出 AI Observation 项目的完整系统流程图，梳理清楚两条自动化流水线的全貌。

**输出：**

用 Mermaid 绘制了覆盖整个项目的 flowchart，包含：

| 模块 | 内容 |
|------|------|
| 触发层 | 每日 09:00 定时 / 每月 1&16 日定时 / 手动触发 |
| Daily Report 流水线 | checkout → 安装依赖 → 检查 key → generate_daily.py → build_daily_index.py → commit push |
| generate_daily.py 内部 | fetch_rss + fetch_snapshot + config + tracking registry + prompt → AI 调用 → .md + .json 双格式输出 |
| Tracking Update 流水线 | checkout → 安装依赖 → 检查 key → update_tracking.py → build_tracking_json.py → commit push |
| update_tracking.py 内部 | 扫描 RSS → 统计高频 → AI 评估准入 → 更新 people/sources/update-reports |
| 数据存储 | 01-daily-reports / 03-tracking-registry / 04-opinion-log / ui/data |
| 前端展示 | index / report / tracking / opinions / research 5 个页面 |

**价值：** 第一次把整个系统完整梳理成一张图，后续扩展/排查问题有全局参照。

---

## 踩坑记录

无新增技术坑。

---

## 被纠正记录

**skill 加载机制认知错误（高价值纠正）**

- ❌ 错误认知：「jason-brain skill 说的步骤零是新会话自动执行」
- ✅ 正确理解：
  - skill 本身没有「新会话自动加载」机制，必须由触发词或人工触发 `use_skill` 后才加载
  - **加载后**的步骤零（读 MEMORY.md + gaps.md）才是自动执行的
  - 两件事不一样：「谁来触发加载」 vs 「加载后自动干什么」
- **行动：** 后续每次新会话，如果哥有开发任务，我应主动判断是否需要加载 jason-brain，不等触发词

---

## 下一步待办

- [ ] 将「每次新会话主动加载 jason-brain」写入 Memory，形成机制
- [ ] 验证 OpenRouter 排行榜 GitHub Actions 无人值守运行是否正常（昨天的待办）
- [ ] 追踪人物观点模块（Level 2 信息源接入后）
